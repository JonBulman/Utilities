[project]
name = "jon_bulman_utilities"
version = "0.1.0"
description = "Jon's shared utilities"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

